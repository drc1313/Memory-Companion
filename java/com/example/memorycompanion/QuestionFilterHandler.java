package com.example.memorycompanion;

import java.util.ArrayList;
import java.util.List;

public class QuestionFilterHandler
{

    QuestionFilterHandler(){}

    boolean createNewFilter(String t, long[] dRange, int[] correctPRange, int streak, String iCats,
                            String eCats, String iKeywords, String  eKeywords)
    {
        boolean validFilter = false;

        return  validFilter;
    }

    //Used to split keywords of a string as well as validate them to specifications
    List<String> stringSplitter(String str)
    {
        List<String> returnList = new ArrayList<>();
        if(str.length() > 0)
        {

        }
        return returnList;
    }

    List<String> filters = new ArrayList<>();

}
